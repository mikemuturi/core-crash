#include <stdio.h>


int result; //global variable. 

void main()
{
	//local variable can only be used inside the function declared in
	//global variable can be used/recallled in any function within the project
	//global variable enhance code reusability.
	//global variable are declared outside functions and called inside functions inside the prohects
	int result; //example of local variable



}
